import 'package:flutter/material.dart';

const primaryColor = Color(0xFF2697FF);
const secondaryColor = Color(0xFF2A2D3E);
const bgColor = Color(0xFF212121);

const defaultPadding = 16.0;
const cardBgColor = Color(0xFF2A2D3E);
const cardBorderRadius = 10.0;

const errorColor = Color(0xFFFF0000);
const warningColor = Color(0xFFFFA500);

// Form styling
const formInputBgColor = Color(0xFF1A1C24);
const formBorderColor = Color(0xFF2E3239);
const formLabelColor = Color(0xFF9FA5B5);

// Custom shadows
const cardShadow = [
  BoxShadow(
    color: Color(0x1A000000),
    blurRadius: 10,
    offset: Offset(0, 4),
  ),
];

// Custom text styles
const formLabelStyle = TextStyle(
  color: formLabelColor,
  fontSize: 14,
  fontWeight: FontWeight.w500,
);

const sectionTitleStyle = TextStyle(
  fontSize: 16,
  fontWeight: FontWeight.bold,
  color: Colors.white70,
);
