import 'package:flutter/material.dart';

enum DashboardPage {
  dashboard,
  predict,
  predictDemand,
  products,
  inbox,
  orderLists,
  productStock,
  calendar,
  contact,
  settings,
  aiAssistant,
  logout
}
