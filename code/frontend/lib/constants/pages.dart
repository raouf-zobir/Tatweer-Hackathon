import 'package:flutter/material.dart';

enum DashboardPage {
  dashboard,
  predict,  // Added predict page
  products,
  inbox,
  orderLists,
  productStock,
  calendar,
  contact,
  settings,
  logout,
}
