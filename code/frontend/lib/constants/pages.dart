import 'package:flutter/material.dart';

enum DashboardPage {
  dashboard,
  predict,
  predictDemand,
  predictCommands,
  gallery,
  products,
  inbox,
  orderLists,
  productStock,
  calendar,
  contact,
  settings,
  logout,
}
